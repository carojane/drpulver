<h1><%= @assignment.title %></h1>
<h3><%= @assignment.course.name %> <small><%= @assignment.course.number %></small></h3>
<h3>Due: <%= @assignment.due_date.strftime("%b %-d, %Y") %></h3>
<p><%= @assignment.body %></p>
